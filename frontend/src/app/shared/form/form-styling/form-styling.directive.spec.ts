import { FormStylingDirective } from './form-styling.directive';

describe('FormStylingDirective', () => {
  it('should create an instance', () => {
    const directive = new FormStylingDirective();
    expect(directive).toBeTruthy();
  });
});
