import { HttpConnectionErrorDirective } from './http-connection-error.directive';

describe('HttpConnectionErrorDirective', () => {
  it('should create an instance', () => {
    const directive = new HttpConnectionErrorDirective();
    expect(directive).toBeTruthy();
  });
});
